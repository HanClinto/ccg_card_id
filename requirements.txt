# Core pipeline dependencies
tqdm
requests
orjson
numpy
Pillow
imagehash
opencv-contrib-python
python-dateutil

# ClintUtils (sibling repo — install separately with: pip install -e ../ClintUtils)
# ClintUtils

# Optional: DINOv2 embeddings
# pip install -e ".[dinov2]" or manually:
# torch
# transformers
